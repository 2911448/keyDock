# 更新记录

## 1.0.4

- 原生隐藏请求失败或未生效时，增加辅助功能接口回退，并确认目标应用实际进入隐藏状态。
- 隐藏期间防止同一应用重复调用；下一次有效调用清除旧错误。
- 增加隐藏失败、延迟、权限拒绝和应用退出的回归测试；共 25 项测试通过。

## 1.0.3

- 修复非激活键盘面板与应用失活自动隐藏之间的冲突。
- 面板失去键盘焦点时同步收起；绑定选择器接管焦点时保留父面板。

## 1.0.2

- 设置页改为普通独立窗口，可关闭和最小化，切换其他应用时不再置顶。
- 设置页失焦后恢复快捷键监听。
- 构建默认使用已有 Apple Development 签名身份，阻止覆盖运行中的应用；临时签名需显式选择。

## 1.0.1

- 根据实际全局事件监听的创建与启用结果展示状态。
- 增加权限变化、睡眠唤醒后的恢复检查，以及手动重新检测入口。
- 增加当前运行期间的事件数、双击次数和最近操作诊断。

## 1.0.0

- 首版原生菜单栏应用：键盘式应用绑定、统一前缀启动、双击修饰键呼出面板。
- 支持显示／隐藏切换、应用搜索与手动选择、登录时启动及本地 JSON 配置。

各版本实测范围与待验收场景见 [验证记录](docs/QA.md)。
